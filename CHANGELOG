v0.2.2. Fixed Linux behavior when no reader is connected.

v0.2.1. Client returns nil instead of exception when it fails to connect to the server.  

v0.2. The multi_proxy can respond to remote queries on connected TEMs.

v0.1. Initial release. Standalone manager works.
